"""
auth_debug.py — removed.
All authentication is handled by the external AI-Being backend.
This stub is kept to avoid ImportError if referenced anywhere.
"""
from fastapi import APIRouter

router = APIRouter()
