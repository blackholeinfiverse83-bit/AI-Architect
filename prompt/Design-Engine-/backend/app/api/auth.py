"""
Auth API - placeholder stub.
All authentication is handled by the external AI-Being backend.
This router is kept to avoid import errors in main.py; it exposes no active endpoints.
"""
from fastapi import APIRouter

router = APIRouter()
