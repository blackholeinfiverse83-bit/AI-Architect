# API Endpoints

This file has been superseded by [`API_REFERENCE.md`](API_REFERENCE.md).

`API_REFERENCE.md` contains the complete, accurate endpoint reference including:
- All production endpoints with correct paths
- Authentication requirements
- Request and response schemas
- Error codes

> Note: `POST /api/v1/generate` and `POST /api/v1/geometry/generate` listed in
> the previous version of this file are **permanently blocked (403)**. The only
> valid design generation entry point is `POST /api/v1/core/generate`.
